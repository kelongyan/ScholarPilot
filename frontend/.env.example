# Backend API base URL. Defaults to the local FastAPI dev server.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional Bearer token sent to the backend when AUTH_ENABLED=true.
NEXT_PUBLIC_KAIROS_AUTH_TOKEN=
